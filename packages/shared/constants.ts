export const DEFAULT_CURRENCY = "INR";

export const AI_CONFIDENCE_REVIEW_THRESHOLD = 0.7;

export const JWT_ACCESS_EXPIRES_IN_DEFAULT = "15m";
export const JWT_REFRESH_EXPIRES_IN_DEFAULT = "7d";

export const PASSWORD_MIN_LENGTH = 8;

export const TRANSACTIONS_DEFAULT_PAGE_SIZE = 20;

export const BUDGET_PERIODS = ["monthly", "weekly"] as const;

export const INPUT_TYPES = ["share_text", "screenshot", "manual"] as const;

export const AUTH_PROVIDERS = ["email", "google"] as const;
